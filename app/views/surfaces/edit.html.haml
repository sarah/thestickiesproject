-action = @surface.user ? user_surface_path(@surface.user, @surface) : surface_path(@surface)
-semantic_form_for @surface, :url => action do |form|
  -form.inputs do
    = form.input :name
  = form.buttons
